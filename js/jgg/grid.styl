nBgc(num)
  for i in 1..num
    &:nth-child({i})
      border-bottom 0
      background-color rgba(n*20,n*5,n*9,n*0.1)
.box
  color white
  display: flex
  align-items center
  justify-content center
  nBgc(9)